# paint-background demo

This Vite + React demo imports the library from the repo root (`file:../..`) and showcases the new dynamic palette support. Select one of the predefined palettes or write your own comma-separated HEX list to feed colors into the shader in real time.

Run:

```bash
cd examples/demo
yarn install
yarn dev
```

Open http://localhost:5173
